'use strict';

/**
 * pages-destination controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::pages-destination.pages-destination');
