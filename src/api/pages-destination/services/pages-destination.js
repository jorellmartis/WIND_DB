'use strict';

/**
 * pages-destination service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::pages-destination.pages-destination');
